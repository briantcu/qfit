<%= form_for(@warmup) do |f| %>
  <% if @warmup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@warmup.errors.count, "error") %> prohibited this warmup from being saved:</h2>

      <ul>
      <% @warmup.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :is_stretch %><br>
    <%= f.check_box :is_stretch %>
  </div>
  <div class="field">
    <%= f.label :video_link %><br>
    <%= f.text_field :video_link %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
