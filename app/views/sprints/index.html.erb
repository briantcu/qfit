<h1>Listing sprints</h1>

<table>
  <thead>
    <tr>
      <th>Difficulty</th>
      <th>Name</th>
      <th>Sprint type</th>
      <th>Num laps</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @sprints.each do |sprint| %>
      <tr>
        <td><%= sprint.difficulty %></td>
        <td><%= sprint.name %></td>
        <td><%= sprint.sprint_type %></td>
        <td><%= sprint.num_laps %></td>
        <td><%= link_to 'Show', sprint %></td>
        <td><%= link_to 'Edit', edit_sprint_path(sprint) %></td>
        <td><%= link_to 'Destroy', sprint, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Sprint', new_sprint_path %>
