<%= form_for(@plyometric) do |f| %>
  <% if @plyometric.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@plyometric.errors.count, "error") %> prohibited this plyometric from being saved:</h2>

      <ul>
      <% @plyometric.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :video_link %><br>
    <%= f.text_field :video_link %>
  </div>
  <div class="field">
    <%= f.label :recommended %><br>
    <%= f.text_area :recommended %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
