<%= form_for(@program_type) do |f| %>
  <% if @program_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@program_type.errors.count, "error") %> prohibited this program_type from being saved:</h2>

      <ul>
      <% @program_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :program_type %><br>
    <%= f.text_field :program_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
