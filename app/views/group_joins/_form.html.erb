<%= form_for(@group_join) do |f| %>
  <% if @group_join.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group_join.errors.count, "error") %> prohibited this group_join from being saved:</h2>

      <ul>
      <% @group_join.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :group_id %><br>
    <%= f.number_field :group_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
