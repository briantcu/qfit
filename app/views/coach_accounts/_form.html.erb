<%= form_for(@coach_account) do |f| %>
  <% if @coach_account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@coach_account.errors.count, "error") %> prohibited this coach_account from being saved:</h2>

      <ul>
      <% @coach_account.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :num_accts %><br>
    <%= f.number_field :num_accts %>
  </div>
  <div class="field">
    <%= f.label :billing_email %><br>
    <%= f.text_field :billing_email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
