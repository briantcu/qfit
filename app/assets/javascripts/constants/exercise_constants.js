var keyMirror = require('helpers/KeyMirror');

module.exports = keyMirror({
    GET_EXERCISES: null,
    STRENGTH_LOADED: null,
    WARMUPS_LOADED: null,
    PLYOS_LOADED: null,
    SPRINTS_LOADED: null
});