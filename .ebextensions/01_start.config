files:
  /etc/my_init.d/start_app.sh:
    mode: "000755"
    owner: root
    group: root
    content: |
      #!/bin/bash
      t
      chmod -R 777 log
      chmod -R 777 tmp
      bundle exec sidekiq -C config/sidekiq.yml &